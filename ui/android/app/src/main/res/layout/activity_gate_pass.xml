<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="36dp"
    android:background="#0A0F1E">

    <!-- Bell emoji -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🔔"
        android:textSize="72sp"
        android:layout_marginBottom="16dp" />

    <!-- Gold badge label -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="GATE PASS REQUEST"
        android:textColor="#C9A84C"
        android:textSize="11sp"
        android:textStyle="bold"
        android:letterSpacing="0.12"
        android:layout_marginBottom="20dp"
        android:background="@drawable/badge_background"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="6dp"
        android:paddingBottom="6dp" />

    <!-- Visitor name / title -->
    <TextView
        android:id="@+id/tv_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Visitor at Gate"
        android:textColor="#FFFFFF"
        android:textSize="26sp"
        android:textStyle="bold"
        android:gravity="center"
        android:layout_marginBottom="12dp" />

    <!-- Visitor details / body -->
    <TextView
        android:id="@+id/tv_body"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text=""
        android:textColor="#8BACC8"
        android:textSize="15sp"
        android:gravity="center"
        android:lineSpacingMultiplier="1.4"
        android:layout_marginBottom="44dp" />

    <!-- Open app button — gold -->
    <Button
        android:id="@+id/btn_open_app"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="Open App to Respond"
        android:textColor="#0A0F1E"
        android:textStyle="bold"
        android:textSize="15sp"
        android:backgroundTint="#C9A84C"
        android:layout_marginBottom="12dp" />

    <!-- Dismiss button — subtle -->
    <Button
        android:id="@+id/btn_dismiss"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:text="Dismiss"
        android:textColor="#8BACC8"
        android:textStyle="bold"
        android:backgroundTint="#111827" />

</LinearLayout>
